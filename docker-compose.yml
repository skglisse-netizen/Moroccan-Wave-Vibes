services:
  app:
    build: .
    container_name: surfschool-app
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
